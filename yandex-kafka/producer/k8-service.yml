apiVersion: v1
kind: Service
metadata:
  name: kafka-service-producer-afonin
spec:
  selector:
    app: kafka-producer-afonin
  ports:
  - port: 80 #external(exposed)
    targetPort: 5000 #internal
  type: NodePort
