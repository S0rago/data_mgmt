apiVersion: v1
kind: Service
metadata:
  name: kafka-cons-service-afonin
spec:
  selector:
    app: kafka-cons-afonin
  ports:
  - port: 80 #external(exposed)
    targetPort: 5000 #internal
  type: NodePort
