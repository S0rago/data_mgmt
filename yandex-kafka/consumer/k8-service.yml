apiVersion: v1
kind: Service
metadata:
  name: kafka-service-consumer-afonin
spec:
  selector:
    app: kafka-consumer-afonin
  ports:
  - port: 80 #external(exposed)
    targetPort: 5000 #internal
  type: NodePort